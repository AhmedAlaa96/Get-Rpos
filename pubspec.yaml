name: candidate_mobile_application
description: Get Repo Mobile application

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.2
  json_annotation: ^3.0.1
  date_format: ^1.0.9
  url_launcher: ^5.5.0
  auto_size_text: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.1
  json_serializable: ^3.3.0


flutter:
  uses-material-design: true
